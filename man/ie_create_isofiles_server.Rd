% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_run.R
\name{ie_create_isofiles_server}
\alias{ie_create_isofiles_server}
\title{Create an isofiles server app for the isoexplorer GUI}
\usage{
ie_create_isofiles_server(
  timezone = Sys.timezone(),
  options = list(),
  uiPattern = "/",
  enableBookmarking = "url",
  default_theme = app_themes(),
  upload_folder = NULL,
  monitoring_folders = NULL,
  examples_folder = "examples",
  temporary_storage = FALSE,
  max_upload_size = NULL,
  log_level = "TRACE"
)
}
\arguments{
\item{timezone}{the timezone to use for datetime display}

\item{options}{Named options that should be passed to the \code{runApp} call
(these can be any of the following: "port", "launch.browser", "host", "quiet",
"display.mode" and "test.mode"). You can also specify \code{width} and
\code{height} parameters which provide a hint to the embedding environment
about the ideal height/width for the app.}

\item{uiPattern}{A regular expression that will be applied to each \code{GET}
request to determine whether the \code{ui} should be used to handle the
request. Note that the entire request path must match the regular
expression in order for the match to be considered successful.}

\item{enableBookmarking}{Can be one of \code{"url"}, \code{"server"}, or
\code{"disable"}. The default value, \code{NULL}, will respect the setting from
any previous calls to  \code{\link[shiny:enableBookmarking]{enableBookmarking()}}. See \code{\link[shiny:enableBookmarking]{enableBookmarking()}}
for more information on bookmarking your app.}

\item{default_theme}{the default bslib Bootstrap 5 theme preset}

\item{upload_folder}{upload directory for the navbar upload button; \code{NULL}
(the default) means no upload button, a path enables it; see \code{\link[=ie_file_server]{ie_file_server()}}}

\item{monitoring_folders}{folders to watch for new isofiles, read and added
automatically (\code{NULL} = off); see \code{\link[=ie_file_server]{ie_file_server()}}}

\item{examples_folder}{directory the "Load examples" navbar button copies the
isoreader2 bundled example files into and loads; \code{"examples"} by default
(\code{NULL} hides the button)}

\item{temporary_storage}{if \code{TRUE}, the upload dialog notes that uploaded files
are stored only for the duration of the session (informational; default
\code{FALSE})}

\item{max_upload_size}{maximum per-file upload size in MB (sets the
\code{shiny.maxRequestSize} option); \code{NULL} (the default) keeps Shiny's ~5 MB
default. Raw isofiles are often larger, so raise this when allowing uploads.}

\item{log_level}{how verbosely the app logs; see \code{\link[=ie_run_app]{ie_run_app()}}. Defaults to
\code{"TRACE"} (log everything) for this server app, unlike the focused explorers
which default to \code{"WARN"}.}
}
\value{
a \code{\link[shiny:shinyApp]{shiny::shinyApp()}} object (unrun)
}
\description{
Builds the full isoexplorer Shiny app -- one navbar tab per measurement type
(continuous flow / dual inlet / scans), each with a file-selector sidebar and
plot -- and returns it as a \code{\link[shiny:shinyApp]{shiny::shinyApp()}} object to run or deploy. Unlike
the focused \code{\link[=ie_explore_continuous_flow]{ie_explore_continuous_flow()}} explorers, this app does NOT take an
\code{ir_isofiles} object -- data arrives at runtime via the navbar \strong{Upload}
button, any watched \code{monitoring_folders}, and/or the \strong{Load examples} button (a
"get started" prompt is shown until something is loaded). Loaded examples are
selected automatically; uploaded files are selected only when the upload modal's
"Select the uploaded files" box is checked.
}
\details{
Because it returns the app object unrun, launch it yourself (print it, or wrap
it in \code{\link[shiny:runApp]{shiny::runApp()}}) or hand it to a deployment tool (e.g. shinyapps.io /
ShinyProxy).
}
\examples{
if (interactive()) {
  # build the full multi-tab server app and run it; load data via the navbar
  # "Load examples" / "Upload" buttons at runtime
  ie_create_isofiles_server() |> shiny::runApp()

  # enable uploads (raise the per-file cap for large raw isofiles)
  app <- ie_create_isofiles_server(
    upload_folder = "uploads",
    max_upload_size = 200
  )
  shiny::runApp(app)
}
}
