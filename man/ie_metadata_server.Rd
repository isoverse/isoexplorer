% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_module_metadata_server.R
\name{ie_metadata_server}
\alias{ie_metadata_server}
\title{Metadata selector module}
\usage{
ie_metadata_server(
  id,
  get_metadata,
  set_selected = NULL,
  get_selection = NULL,
  get_select_signal = NULL
)
}
\arguments{
\item{id}{the module id (must match the paired \code{\link[=ie_metadata_ui]{ie_metadata_ui()}})}

\item{get_metadata}{a reactive returning the metadata tibble to browse (one
row per analysis, with \code{uidx} + \code{analysis} columns)}

\item{set_selected}{optional \verb{function(rows)} called with the selected
metadata rows whenever the selection changes (e.g.
\code{file$set_selected_scans})}

\item{get_selection}{optional reactive of the current selection, used to
reflect it in the table on load (e.g. \code{file$get_scans_selection})}

\item{get_select_signal}{optional reactive carrying file paths the table should
exclusively select (e.g. \code{file$get_scans_select_signal}, fired by upload
auto-select); applied once the table contains those files}
}
\value{
a list with the underlying selector-table handle plus the
\code{get_selected_row_id} / \code{get_selected_metadata} reactives
}
\description{
Server for a file/analysis selector table (pair with \code{\link[=ie_metadata_ui]{ie_metadata_ui()}}). It
shows a row-grouped table over an already-aggregated metadata tibble and
pushes the current selection out via \code{set_selected}; double-clicking a row
selects just that analysis, double-clicking a file's group header selects all
of its analyses. Most users want the typed wrappers
\code{\link[=ie_scans_metadata_server]{ie_scans_metadata_server()}} / \code{\link[=ie_cf_metadata_server]{ie_cf_metadata_server()}} / \code{\link[=ie_di_metadata_server]{ie_di_metadata_server()}},
which bind the right \code{\link[=ie_file_server]{ie_file_server()}} accessors; \code{ie_metadata_server()} itself is
the generic version for wiring a custom metadata source.
}
\examples{
if (interactive()) {
  library(shiny)
  iso <-
    isoreader2::ir_examples_folder() |>
    isoreader2::ir_find_isofiles() |>
    isoreader2::ir_read_isofiles()

  ui <- ie_metadata_ui("meta")
  server <- function(input, output, session) {
    file <- ie_file_server("files", get_isofiles = reactive(iso))
    # the generic selector wired to any aggregated-metadata accessors; most
    # users want the typed wrappers ie_scans_metadata_server() / _cf_ / _di_
    ie_metadata_server(
      "meta",
      get_metadata = file$get_scans_metadata,
      set_selected = file$set_selected_scans,
      get_selection = file$get_scans_selection
    )
  }
  shinyApp(ui, server)
}
}
