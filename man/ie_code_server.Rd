% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_module_code.R
\name{ie_code_server}
\alias{ie_code_server}
\alias{ie_code_ui}
\title{Code-generation server}
\usage{
ie_code_server(id, get_active_group = reactive(NULL))

ie_code_ui(id)
}
\arguments{
\item{id}{the module id (namespace); pair with \code{\link[=ie_code_ui]{ie_code_ui()}} on the same id}

\item{get_active_group}{a reactive returning the active group id; only
registrations in this group (plus group-less ones) are shown. The default
\code{reactive(NULL)} shows everything.}
}
\value{
the code handle: a list with \code{register(code_id, heading, get_code, depends_on = NULL, group = NULL)} (each \code{depends_on} is a single id -- the
tree is single-parent) and \code{build_document(quarto = FALSE)} (assemble +
return \code{list(script, headings)}; exposed mainly for testing).
}
\description{
A central module that assembles "idealized example code" for an isoexplorer
app and shows it in a viewer. It is instantiated once at the top level; every
other module that can contribute code returns a \code{get_code()} generator, and the
top-level wiring \strong{registers} each one here with \code{register()} under an id, a
heading, and (optionally) the id it \code{depends_on}.
}
\details{
The registrations form a dependency tree. When the user clicks the navbar
\strong{Show code} button (\code{\link[=ie_code_ui]{ie_code_ui()}}), the registered generators are walked
depth-first and assembled into one document: the tree depth sets the heading
level (\verb{#} for a root such as a read step, \verb{##} for a step depending on a root,
\verb{###} for one depending on that, ...), and each step's \code{output} variable is
threaded in as the \code{input_var} of the steps that depend on it.
The document is shown in a read-only \code{\link[shinyAce:aceEditor]{shinyAce::aceEditor()}} with a clickable
headings tree (jump-to-section), a toggle between plain code and a Quarto view,
and a \code{.qmd} download.

Each registration may carry a \code{group}; when \code{get_active_group()} returns a
non-empty value only registrations in that group (plus group-less ones) are
shown -- the full multi-tab app uses this to show just the active measurement
type's code.
}
\section{Functions}{
\itemize{
\item \code{ie_code_ui()}: the navbar \strong{Show code} button (place in the navbar,
e.g. \code{bslib::nav_item(ie_code_ui("code"))}) plus the one-time JS used to jump
the editor to a heading. Pair with \code{\link[=ie_code_server]{ie_code_server()}} on the same \code{id}.

}}
\section{The \code{get_code()} contract}{
 a registered generator is
\verb{function(input_var = NULL)} returning \verb{list(code = <string>, output = <string or NULL>)}. It is called during assembly inside a reactive context,
so it reflects the module's \emph{current} state; \code{input_var} is the output
variable of the module it depends on (\code{NULL} for a root), and \code{output} is the
variable this snippet binds (or \code{NULL} for a terminal node such as a plot).
}

