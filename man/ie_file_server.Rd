% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_module_file.R
\name{ie_file_server}
\alias{ie_file_server}
\alias{ie_file_ui}
\title{Central file-management module}
\usage{
ie_file_server(
  id,
  get_isofiles,
  initial_selection = "all",
  upload_folder = NULL,
  monitoring_folders = NULL,
  examples_folder = NULL
)

ie_file_ui(id)
}
\arguments{
\item{id}{the module id (namespace)}

\item{get_isofiles}{a reactive returning an \code{ir_isofiles} to seed the app with
(already read); \code{reactive(NULL)} is fine when files only arrive via upload /
monitoring}

\item{initial_selection}{what is selected, per type, before any selector pushes
a selection. One of \code{"all"} (default), \code{"none"}, or a \verb{function(metadata)}
called with a type's metadata tibble that returns the subset of rows to
select (e.g. \verb{\\(m) dplyr::filter(m, grepl("std", file_name))}).}

\item{upload_folder}{directory where uploaded files are stored (created on
demand); \code{NULL} (the default) means no upload button -- set a directory path
to enable uploads.}

\item{monitoring_folders}{character vector of folders to watch; isofiles found
there with \code{\link[isoreader2:ir_find_isofiles]{isoreader2::ir_find_isofiles()}} are read and added automatically.
\code{NULL} (default) disables monitoring.}

\item{examples_folder}{directory the "Load examples" navbar button copies the
isoreader2 bundled example files into (and then loads). \code{NULL} (the default)
means no examples button.}
}
\value{
The "file handle": a list of reactive accessors / setters. For each
\verb{<type>} in \code{scans} / \code{cf} / \code{di}: \code{get_units()}/\code{set_units(units)} (shared
intensity units, default "mV"); \verb{get_<type>_metadata()};
\verb{set_selected_<type>(rows)}; \verb{get_<type>_selection()};
\verb{get_aggregated_<type>_data()}; plus \verb{get_<type>_select_signal()} (file paths
a selector should select, fired by upload auto-select) and \code{get_active_type()}
(the type whose tab to activate after an auto-select).
}
\description{
The single source of truth for the isofiles in an isoexplorer app, and the hub
every other module talks to. It maintains a \emph{running} set of read isofiles
(seeded from \code{get_isofiles()}, grown by uploads and watched folders), splits it
into the three measurement types (scans / continuous flow / dual inlet) with
\code{\link[isoreader2:ir_filter_for_scans]{isoreader2::ir_filter_for_scans()}} and friends, owns the shared intensity-units
selection and the per-type file selection, and exposes the per-type metadata
(for the \code{\link[=ie_metadata_server]{ie_metadata_server()}} selector tables) and the selection-filtered
aggregated data (for the \verb{*_plot_server()} modules).
}
\details{
Selector tables read \verb{get_<type>_metadata()} and push their selection via
\verb{set_selected_<type>()}; plot modules read \verb{get_aggregated_<type>_data()} and
drive the shared units via \code{get_units()} / \code{set_units()}.

\strong{Dynamic files.} New files (uploaded, or appearing in \code{monitoring_folders})
are read with \code{\link[isoreader2:ir_read_isofiles]{isoreader2::ir_read_isofiles()}} and appended; aggregation is
incremental (only new files are read/aggregated, then combined with \code{c()}), so
already-read files are never re-read.

\strong{Upload.} When \code{upload_folder} is set the module owns a navbar upload button
(the \code{\link[=ie_file_ui]{ie_file_ui()}} placeholder) that opens a modal to upload multiple files or
whole folders bundled as \code{.zip} archives (the picker allows \code{.zip}, the file
types isoreader2 reads, and \code{.json} -- which covers their \verb{.<type>.json}
serializations such as \code{foo.cf.json}). Uploaded files are stored in
\code{upload_folder} (archives unpacked), and \strong{only the just-uploaded files are
read} -- files already present in \code{upload_folder} when the app started are
left untouched. An "Auto-select the newly uploaded files" checkbox (on by default)
exclusively selects the new files in the relevant type's table and, in the full
multi-tab app, switches to that type's tab.

\strong{Monitoring.} \code{monitoring_folders} are polled; any isofiles found there with
\code{\link[isoreader2:ir_find_isofiles]{isoreader2::ir_find_isofiles()}} (including files already present at startup)
are read and added.

\strong{Getting started.} When \code{examples_folder} and/or \code{upload_folder} is set but the
app is launched without data (empty \code{get_isofiles()}), a prompt is shown once
inviting the user to load the examples and/or upload their own files; an app
launched with data never sees it.
}
\section{Functions}{
\itemize{
\item \code{ie_file_ui()}: the navbar placeholders for the "Load examples" and
"Upload" buttons (each rendered only when the server's \code{examples_folder} /
\code{upload_folder} is set). Pair with \code{\link[=ie_file_server]{ie_file_server()}} on the same \code{id}.

}}
